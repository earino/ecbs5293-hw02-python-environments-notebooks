# Git recovery exercise

Do this only after everything works and is committed.

1. **What did you break, and how?** (Saved, not staged: no `git add` before the restore.)

2. **The exact Git commands that brought it back** (paste them, with their output):

```text

```

3. **What did you run to confirm it was really back?**
